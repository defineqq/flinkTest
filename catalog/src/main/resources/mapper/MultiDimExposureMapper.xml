<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="service.MultiDimExposureMapper">
    <!--查询关键字匹配 -->
    <insert id="insert" parameterType="models.MultiDimExposureModel" >
        insert into multi_dim_exposure (bid, posid,dt, uv,pv)
        value(#{bid},#{posid},#{dt},#{uv},#{pv})
        ON DUPLICATE KEY UPDATE
        uv = uv + #{uv},
        pv = pv + #{pv}
    </insert>
</mapper>